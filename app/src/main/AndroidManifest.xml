<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".idpassportpdf"
            android:exported="false" />
        <activity
            android:name=".registration"
            android:exported="false" />
        <activity android:name=".Create_new_account" />
        <activity android:name=".About_us" />
        <activity android:name=".Feedback" />
        <activity android:name=".Login_page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Payment" />
        <activity android:name=".Profile_page" />
        <activity
            android:name=".Flash_page"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Bus_pass_list"
            android:label="@string/title_activity_bus_pass_list" />
        <activity
            android:name=".Bus_pass_status"
            android:label="@string/title_activity_bus_pass_status" />
        <activity
            android:name=".BusPass_details"
            android:label="@string/title_activity_bus_pass_details" />
        <activity
            android:name=".Apply_bus_pass"
            android:label="@string/title_activity_apply_bus_pass" />
    </application>

</manifest>